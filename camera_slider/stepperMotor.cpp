#include "stepperMotor.h"

// AccelStepper rotateMotor(1, 9, 10); // 1 = driver,  pin 5 = step, pin 4 = direction
// AccelStepper tiltMotor(1, 6, 7); //1 = driver,  pin 11 = step, pin 10 = direction
// AccelStepper slideMotor(1, 3, 4); //1 = driver,  pin 11 = step, pin 10 = direction


// StepperMotor::StepperMotor(uint8_t steppin, uint8_t dirpin) {
//     stepPin = steppin;
//     dirPin = dirpin;

//     AccelStepper motor=AccelStepper(1, stepPin, dirPin); // 1 = motor driver (Pololu)
//     motor.setMaxSpeed(400);
//     motor.setSpeed(0);
// }

// StepperMotor::setSpeed(float speed) {
//     motor.setSpeed(speed);
//     motor.runSpeed();
// }
